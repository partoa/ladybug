# json_utils.cpp moved to src/common/
# This file kept for backwards compatibility but no longer builds anything

set(JSON_EXTENSION_OBJECT_FILES
        ${JSON_EXTENSION_OBJECT_FILES}
        PARENT_SCOPE)
